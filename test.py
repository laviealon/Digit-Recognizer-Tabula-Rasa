import os
from image_transformation import convert

if __name__ == '__main__':
    # for dirc in os.listdir('training_data/trainingSet/trainingSet'):
    #     if dirc == '.DS_Store':
    #         continue
    #     for folder in os.listdir('training_data/trainingSet/trainingSet/' + dirc):
    #         print(folder)
    k = [1, 2, 3]
    for i in range(len(k) - 1, -1, -1):
        print('hello')
        print(i)
